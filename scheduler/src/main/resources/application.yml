server:
  port: 8091

spring:
  application:
    name: notification-scheduler
  profiles:
    active: dev

api-key:
  header: X-API-Key
  value:
    notification: ${NOTIFICATION_API_KEY}

endpoint:
  notifier-module:
    notification: /api/notifications
